import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'one.roriri.vr2',
  appName: 'VR2',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
